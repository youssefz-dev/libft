# libft

A custom C library implementing essential functions from the standard C library, along with additional utility functions.

## Features

- String manipulation
- Memory management
- Character checks
- Conversion functions
- Linked list utilities

## Usage

Clone the repository and compile the library:

```bash
git clone <repository-url>
cd libft
make
